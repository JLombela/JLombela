"use client"
import DashboardPage from "./dashboard/page"

export default function Home() {
  // The root page now renders the main dashboard content.
  // Navigation is handled by the AppSidebar in the root layout.
  return <DashboardPage />
}
